{
  "name": "xgodev",
  "owner": {
    "name": "xgodev",
    "url": "https://github.com/xgodev"
  },
  "metadata": {
    "description": "Claude Code plugins shipped from the github.com/xgodev/boost repository.",
    "version": "1.3.0"
  },
  "allowCrossMarketplaceDependenciesOn": ["xgodev"],
  "plugins": [
    {
      "name": "golang-boost",
      "source": "./",
      "description": "Component-aligned skill bundle for github.com/xgodev/boost — 56 skills, one per boost component, cross-referenced via REQUIRED BACKGROUND so Claude loads only what the current task needs. Covers every factory contrib, every wrapper, every bootstrap component, plus a maintainer guide. Pulls in xgodev/quality-gate for the pre-push comparative gate as a dependency.",
      "version": "0.8.1",
      "category": "language-go",
      "tags": ["go", "golang", "boost", "echo", "resty", "pubsub", "cloudevents", "nats", "kafka", "mongo", "cassandra", "redis", "elasticsearch", "aws", "grpc", "fx", "otel", "datadog", "prometheus", "k8s"]
    }
  ]
}
